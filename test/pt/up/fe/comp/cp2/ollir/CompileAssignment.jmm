import A;
import B;

class CompileAssignment extends A{

    public static void main(String[] args) {

    }

    public static int[] foo(int b) {
        A A;
        B b;

        this.lol();
        b = A;
        b = new B();
        A = b;
        A = A.bar(A.ber(A,b),A,b);
        A = b.bar(A.ber(A,b),A,b);

        return 0;
    }
}